package com.trilink.game.ui.theme

import androidx.compose.ui.graphics.Color

// ─── 棋子专用色（不随主题变化，保持辨识度）───

/** X 棋子 — 深蓝 */
val PieceX = Color(0xFF1A56DB)

/** O 棋子 — 深红 */
val PieceO = Color(0xFFDC2626)

/** 空格圆点 */
val DotGray = Color(0xFF9CA3AF)

/** X 浅底色 (Light) */
val PieceXLight = Color(0xFFDBEAFE)

/** O 浅底色 (Light) */
val PieceOLight = Color(0xFFFEE2E2)

/** X 深底色 (Dark) */
val PieceXDark = Color(0xFF1E3A5F)

/** O 深底色 (Dark) */
val PieceODark = Color(0xFF3B1A1A)
